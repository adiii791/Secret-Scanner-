# Normalise line endings to LF in the repository for Linux/AWS compatibility.
* text=auto eol=lf

# Treat these as binary — no line-ending conversion.
*.png  binary
*.jpg  binary
*.ico  binary
*.zip  binary
*.db   binary
